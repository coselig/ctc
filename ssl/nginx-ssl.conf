# server {
#     listen 443 ssl;
#     server_name coselig.com;

#     # SSL 憑證配置
#     ssl_certificate /home/coselig/ctc/ssl/coselig.com+3.pem;
#     ssl_certificate_key /home/coselig/ctc/ssl/coselig.com+3-key.pem;

#     # SSL 設定
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;

#     # Supabase API 代理
#     location /api/ {
#         # 移除 /api 前綴並代理到 Supabase
#         rewrite ^/api/(.*)$ /$1 break;
#         proxy_pass http://coselig.com:8000;
#         proxy_set_header Host coselig.com;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
        
#         # CORS 設定
#         add_header Access-Control-Allow-Origin *;
#         add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
#         add_header Access-Control-Allow-Headers "Content-Type, Authorization, apikey";
        
#         # 處理 OPTIONS 請求
#         if ($request_method = 'OPTIONS') {
#             add_header Access-Control-Allow-Origin *;
#             add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
#             add_header Access-Control-Allow-Headers "Content-Type, Authorization, apikey";
#             return 204;
#         }
#     }

#     # 將所有其他請求代理到 Flutter Web Server
#     location / {
#         proxy_pass http://localhost:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
        
#         # WebSocket 支援（如果需要）
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }

#     # HTTP 重定向到 HTTPS
#     error_page 497 https://$host$request_uri;
# }

# HTTP 到 HTTPS 重定向
server {
    listen 80;
    server_name coselig.com;
    return 301 https://$server_name$request_uri;
}